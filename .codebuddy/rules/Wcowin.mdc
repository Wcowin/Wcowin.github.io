---
description: zensical
alwaysApply: true
enabled: true
updatedAt: 2026-07-31T00:00:00.000Z
provider: 
---

# 博客写作规则

## 架构

我们使用 **Zensical** 框架。

## 文章格式规范

### 加粗标签的换行规则

所有加粗标签必须**单独成行**，后面跟一个**空行**，再写内容。

**正确写法：**

```markdown
**核心优势：**

- 第一点
- 第二点

**主要劣势：**

- 第一点

**适用场景：**

这里是描述文字。
```

**错误写法：**

```markdown
**核心优势：**
- 第一点

**核心优势：**  
- 第一点

**适用场景：** 这里是描述文字。
```

### 适用范围

以下标签必须遵循此规则：

- `**核心优势：**`
- `**主要劣势：**`
- `**适用场景：**`
- `**注：**`
- `**核心洞察：**`
- `**定义：**`
- `**效果：**`
- `**一句话总结：**`
- `**弱示例：**` / `**强示例：**` / `**示例：**`
- `**社区最佳实践：**`
- 其他所有加粗标签（如 `**主要优势：**`、`**核心能力：**` 等）

**例外：** `**本文作者：**` 保持原样（内联 HTML 组件，不拆分）。

### 列表项间距

- 列表项之间不加空行
- 列表块前后各保留一个空行

### 表格格式

- 表格前后各保留一个空行
- 表格内单元格内容简洁

## 发布流程

写完文章后，需要添加导航引用：

**`zensical.toml`** — 在对应分类的 `nav` 中添加文章链接

