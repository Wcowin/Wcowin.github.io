# 2026-09-14

## 博客站点改动（Zensical）

- 启用 `tags` 插件：`zensical.toml` 中 `[project.plugins.tags]` 取消注释；`[project.theme.icon.tag]` 设 `default = "material/tag"`；导航加 `{ "标签" = "tags.md" }`（注意是 `tags.md` 非 `tag.md`，由插件虚拟生成）。全站 180+ 篇文章 front matter 已带 `tags`，启用即生效。本地预览地址为 `/tags/`（非 `/tags.md`，因 `use_directory_urls=true`）。
- 新增「科研」下的同级目录「网络空间安全」：nav 中 `科研` 分组新增 `{ "网络空间安全" = ["research/cybersec/index.md"] }`，与「低空技术与工程」同级；新建 `docs/research/cybersec/index.md` 并写入 6 个占位卡片子主题（密码学基础/Web安全/网络安全协议/系统与主机安全/恶意代码与防护/安全运维与取证），条目暂为纯文本未挂链接。
- 用户偏好：改完代码即可，不要代为跑 `zensical build` / `serve` / 预览。
- nav 重组：把原「技术」下的「密码学」子目录（`blog/Cryptography/*`）移到「科研 → 网络空间安全」下作为子目录，仅改 `zensical.toml` 导航，MD 文件保持原位置不动。现层级：科研 > 网络空间安全 > 密码学。注意 `cybersec/index.md` 里的「密码学基础」占位卡片与真实「密码学」子目录重复，待用户决定是改成链接还是删除。
- nav 重组（续）：把原「技术」下的「Math学术」整块改名为「数学与学术」迁入「科研」下（同级于低空/网安），并把它里原在「闲聊 → 数至十」的「王虹与三维挂谷猜想的解决」(`relax/Math/hong-wang-and-the-3d-kakeya-conjecture.md`) 一并并入「数学与学术」；「数至十」仅保留 1–6。所有 MD 文件位置不动。现「科研」四大块：低空技术与工程 / 网络空间安全(含密码学) / 数学与学术 / UGP。
- `docs/blog/Cryptography/Numbertheory.md`：在「## 1. 基础概念」前新增「## 0.前言」，用小学「数苹果 / 分苹果 / 拆数字」三步把加法、带余除法（同余）、质数与合数串起来，结尾玩梗「至此，你已经学会了基本的数论」，再过渡到密码学语言。
- `docs/blog/Cryptography/ModernCryptography.md` 更新至 2026 年现状：新增 HNDL 威胁与 NIST IR 8547（2030 弃用 / 2035 停用）；补充 HQC（2025.3 第五算法，编码基，2027 定稿）、FN-DSA(FIPS 206) 仍在草案、SP 800-227（2025.9）；新增「从标准到落地：混合密钥交换」小节（RFC 10024 的 X25519MLKEM768 等三组、浏览器/密码库支持、2026 测量：49.3% 混合、证书侧约 0%）；中国部分补 2026 两会王小云「三年内建成国标体系」、无结构格 S-Cloud+、迁移节奏；同态加密补 OpenFHE/TFHE-rs 与硬件加速；ZKP 补 zkVM/递归折叠/证明即服务/zkML 及「不抗量子」提醒；SP 800-232 定稿（2025.8）；时间线补 2025–2035 行；参考资料补 RFC 10024/9954/9794、SP 800-227/232、IR 8547、arXiv:2606.16473。
