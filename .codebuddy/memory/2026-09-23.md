# 2026-09-23

## 密码学系列：KZG 文章改造并入系列

把已有的 `docs/blog/Cryptography/KZG.md` 改造成本系列正式文章：

- 补齐 frontmatter（`title: KZG 多项式承诺` / `date` / `tags: 密码学`），移除原来的 blockquote 元信息头。
- 开头加入系列互链引言（指向 zkSNARK、ModernCryptography），并点明"KZG 是 PCS、不是完整 ZKP"。
- 「四、KZG 的具体构造」前加 mermaid 流程图（Setup→Commit→Open→Verify）。
- 文末加「延伸阅读（本系列）」+ 作者行（沿用系列统一尾部格式）。
- 注册：`docs/blog/Cryptography/index.md`（应用与实践卡片 + 学习路径新增第 8 步）、`zensical.toml` 导航。

## 技术要点（复用）

- 站点数学公式用 KaTeX，`javascripts/katex.js` 已配置 `$$`/`$`/`\[`/`\(` 四种定界符，因此使用 `\[ \]` 的旧文**无需**转成 `$$` 即可正常渲染。
- 系列文章统一尾部：`---` → `## 延伸阅读（本系列）` → `## 引文` → `**本文作者：**` 带 avatar 的 HTML span。

## zkSNARK.md 增补（参照桌面笔记）

参照 `~/Desktop/zk-SNARK-完整理解笔记.md`（原书第 4 章整理版）补全了 `docs/blog/Cryptography/zkSNARK.md` 中间被跳过的推理环节：

- 3.3 新增「第零步：程序→约束」的 if/else 代数化例子、R1CS 三种编码技巧表（比特/定值/位分解）、完整手算数值例子（L=x²-5x+7 等）、以及"不是恒等于 0 而是逐点成立"的易错点。
- 新增 3.5（随机参数分工表 s/ρ/α/β/γ/δ）与 3.6（公开输入 L_v 与秘密输入 L_p 在群上合并）。
- 新增 6.5（协议层三个验证检查：结构/变量一致性/计算正确性 + 群乘 vs 配对的易错区分），原 6.5 顺延为 6.6 Groth16。
- 小结加「组件速查表」；参考资料加 KZG 互链。

## 新增第6章：数字签名与认证协议

`docs/blog/Cryptography/DigitalSignatureAndAuth.md`（已注册进 index.md 新卡片「数字签名与认证」+ zensical.toml 导航，并给第4/5章加了反向互链）。

章节结构：6.1 定义与性质（与公钥加密对偶表、哈希-签名范式）→ 6.2 EUF-CMA/SUF-CMA 安全模型与归约 → 6.3 朴素RSA三个漏洞 + RSA-PSS → 6.4 ECDSA（nonce 重用泄私钥公式、延展性）→ 6.5 Schnorr/EdDSA（Σ协议、Fiat-Shamir、线性性→MuSig2）→ 6.6 特殊签名表（盲/环/群/聚合BLS/多签/门限/一次签名）→ 6.7 NIST 标准化 → 6.8 认证协议（挑战响应、N-S 对称版 + Denning-Sacco、公钥版 + Lowe 攻击、Kerberos）→ 6.9 后量子 → 6.10 陷阱清单。

资料来源：USTC 密码学课程「数字签名与认证协议」章、NIST Digital Signatures 项目页（FIPS 186-5 / SP 800-186 / SP 800-208 / FIPS 204-205）、Systems Approach 8.4 认证协议。

## 用户偏好（延续）

- 文章要从专业角度写，需含**公式 + 可运行代码**，不能只是科普介绍。
- 写完不主动运行构建（按用户偏好）。
